<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzit.model.Student">
    <insert id="insert">
        insert into student values(null,'晴空',25,'女');
    </insert>
    <insert id="insertObject" parameterType="com.xzit.model.Student">
        insert into student values(null,#{name},#{age},#{gender});
    </insert>
    <insert id="insertAndGetId" parameterType="com.xzit.model.Student">
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
            INSERT INTO student values(null,#{name},#{age},#{gender});
    </insert>
    <delete id="delete" parameterType="int">
        delete from student where id=#{id}
    </delete>
    <delete id="deleteByName" parameterType="string">
        delete from student where name=#{name}
    </delete>
    <delete id="deleteByLikeName" parameterType="string">
        delete from student where name like #{name}
    </delete>
    <delete id="deleteByLikeName1" parameterType="string">
        delete from student where name like '${name}%'
    </delete>
    <update id="update" parameterType="com.xzit.model.Student">
        update student
        set name=#{name},
            age=#{age},
            gender=#{gender}
        where id = #{id}
    </update>
    <update id="updateByGender" parameterType="com.xzit.model.Student">
        update student
        set age=#{age}
        where gender=#{gender}
    </update>
    <select id="selectList" resultType="com.xzit.model.Student">
        select *
        from student
    </select>
    <select id="selectById" resultType="com.xzit.model.Student">
        select *
        from student
        where id = #{id}
    </select>
    <select id="selectByCondition" resultType="com.xzit.model.Student">
        select *
        from student
        where gender = #{gender}
        and age > #{age}
    </select>
</mapper>
